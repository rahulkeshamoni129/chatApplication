{
  "name": "chat-app-unified",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix Frontend --production=false && npm run build --prefix Frontend && npm install --prefix Backend",
    "start": "node Backend/index.js",
    "dev": "concurrently \"npm run start --prefix Backend\" \"npm run dev --prefix Frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
